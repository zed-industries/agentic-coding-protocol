{
  "name": "agentic-coding-protocol",
  "version": "0.0.1",
  "description": "The Agentic Coding Protocol (ACP) is a protocol that standardizes communication between *code editors* (interactive programs for viewing and editing source code) and *coding agents* (programs that use generative AI to autonomously modify code).",
  "homepage": "https://github.com/zed-industries/agentic-coding-protocol#readme",
  "bugs": {
    "url": "https://github.com/zed-industries/agentic-coding-protocol/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zed-industries/agentic-coding-protocol.git"
  },
  "license": "MIT",
  "author": "Zed Industries",
  "type": "module",
  "main": "dist/acp.js",
  "types": "dist/acp.d.ts",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "clean": "rm -rf dist target typescript/schema.ts schema.json && cargo clean",
    "test": "cargo test && vitest run",
    "generate": "cargo run && typescript/generate.js && prettier --write typescript/schema.ts schema.json",
    "build": "npm run generate && tsc",
    "lint": "eslint && cargo clippy",
    "lint:fix": "eslint --fix && cargo clippy --fix",
    "format": "prettier --write . && cargo fmt",
    "format:check": "prettier --check . && cargo fmt -- --check",
    "check": "npm run lint && npm run format:check && npm run build && npm run test"
  },
  "devDependencies": {
    "@eslint/js": "^9.29.0",
    "@types/node": "^24.0.3",
    "@typescript-eslint/eslint-plugin": "^8.35.0",
    "@typescript-eslint/parser": "^8.35.0",
    "@vitest/ui": "^3.2.4",
    "eslint": "^9.29.0",
    "globals": "^16.2.0",
    "json-schema-to-typescript": "^15.0.4",
    "prettier": "^3.3.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
